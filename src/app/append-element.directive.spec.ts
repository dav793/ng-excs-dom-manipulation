import { AppendElementDirective } from './append-element.directive';

describe('AppendElementDirective', () => {
  it('should create an instance', () => {
    const directive = new AppendElementDirective();
    expect(directive).toBeTruthy();
  });
});
